import 'package:flutter/material.dart';
import '../../domain/entities/service_request.dart';

/// TODO(ui): implemented by feature agent — placeholder stub.
class NewServiceRequestScreen extends StatelessWidget {
  const NewServiceRequestScreen({super.key, required this.kind});
  final SrKind kind;

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('NewServiceRequestScreen')));
  }
}
